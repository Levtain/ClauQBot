# Contributing to ClauQBot

感谢您对 ClauQBot 的关注！我们欢迎任何形式的贡献。

## 如何贡献

### 报告问题

如果您发现了bug或有功能建议，请：

1. 检查 [Issues](https://github.com/yourusername/claude-qq-bridge/issues) 看是否已有相关讨论
2. 如果没有，创建新的Issue，包含：
   - 清晰的标题
   - 详细的问题描述
   - 复现步骤
   - 预期行为和实际行为
   - 环境信息（Python版本、操作系统等）
   - 相关的日志或截图

### 提交代码

1. **Fork** 本仓库
2. 创建您的特性分支：`git checkout -b feature/AmazingFeature`
3. **提交** 您的更改：`git commit -m 'Add some AmazingFeature'`
4. **推送到** 您的分支：`git push origin feature/AmazingFeature`
5. 提交 **Pull Request**

### 代码规范

- 遵循 PEP 8 Python代码规范
- 添加必要的注释和文档字符串
- 确保代码通过flake8检查
- 为新功能添加测试

### 提交信息规范

使用清晰的提交信息：

```
type(scope): subject

body

footer
```

类型：
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 添加测试
- `chore`: 构建/工具链更新

示例：
```
feat(bot): 添加心跳检测功能

定期检查OneBot连接状态，检测QQ掉线情况
- 每60秒发送一次状态查询
- 连续3次失败判定为掉线
- 在WebUI显示连接状态

Closes #123
```

## 开发环境设置

### 1. Fork并克隆仓库

```bash
git clone https://github.com/yourusername/claude-qq-bridge.git
cd claude-qq-bridge
```

### 2. 创建虚拟环境

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate  # Windows
```

### 3. 安装依赖

```bash
pip install -r requirements.txt
pip install pytest pytest-asyncio flake8  # 开发依赖
```

### 4. 运行测试

```bash
pytest
```

### 5. 代码检查

```bash
flake8 src/
```

## 项目结构

```
claude-qq-bridge/
├── src/              # 核心代码
├── api/              # FastAPI后端
├── webui/            # Streamlit前端
├── tests/            # 测试文件
├── .github/          # GitHub Actions配置
└── docs/             # 文档
```

## 功能开发路线图

### 优先级P0（核心功能）
- [ ] 消息队列（并发处理）
- [ ] 会话持久化
- [ ] 错误重试机制
- [ ] 心跳检测

### 优先级P1（体验优化）
- [ ] 日志查看界面
- [ ] 成本统计和预警
- [ ] 权限控制（白名单/黑名单）
- [ ] NapCat状态监控

### 优先级P2（高级功能）
- [ ] 多项目支持
- [ ] 插件系统
- [ ] Docker部署
- [ ] 多平台支持

## 许可证

通过贡献代码，您同意您的贡献将在 MIT License 下发布。

## 联系方式

- GitHub Issues: [项目Issues页面]
- Discord: (待添加)
- Email: (待添加)

---

再次感谢您的贡献！🙏
